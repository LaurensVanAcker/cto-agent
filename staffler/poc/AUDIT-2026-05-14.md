# Mockup ↔ PoC audit, 2026-05-14

End-of-day deep compare between the mockups under `/staffler/mockups/`
and the dps-clone PoC after the pilot-feedback round + gap-closure round.

Format: findings are listed as ✅ (matches), ⚠️ (cosmetic gap), or 🔵
(behaviour gap — needs work).

## Mockup 09 — Nieuwe shift (dialog-volledig)

PoC entry: `dialog-shift-batch/`.

| Element | Status | Notes |
|---|---|---|
| Header subtitle "Klant: X" | ⚠️ | Intentionally omitted — operator already sees the company name in the topbar. Keep as-is. |
| "Bestaande shift gebruiken / Nieuwe uren" toggle | ✅ | Wired via `useExistingShift` signal. |
| Shift template row (read-only input + clear + chevron) | ⚠️ | Impl uses `<p-select>` which carries a native dropdown chevron but no separate clear button. Acceptable since PrimeNG handles clear via the empty option. |
| Werkuren / Pauze time-pair rows | ✅ | Identical layout. |
| Slot list with numbered badge | ✅ | `m09-num` in dialog-shift-batch. |
| Vast-tag (`vast-tag`) for permanent slots | ✅ | `m09-vast-tag` class. |
| Hoverable open-slot with "Persoon / Open" choice | ✅ | `kind === 'open'` slot reveals the binary buttons. |
| "+ Shift toevoegen" button | ✅ | `m09-add-slot`. |
| Naam-dropdown grouped by section (e.g. "Toog Gent (3)") | 🔵 | Impl uses flat `<p-select>` with search filter. Mockup groups by service-location with per-group counts. PrimeNG p-select supports `optionGroupLabel`/`optionGroupChildren` — concrete v1 enhancement. |
| Loonpakket mismatch banner (per slot, Locaties only) | ✅ | `shouldShowLoonpakketBanner` (item 4). |
| "Bevestig shift" CTA | ✅ | `m09-btn-confirm`. |

## Mockup 11 — Planning Locaties (planning-vsl)

PoC entry: `planning-poc/` Locaties view.

| Element | Status | Notes |
|---|---|---|
| Top toolbar — view toggle (Medewerkers/Locaties) | ✅ | item 3 rename. |
| Zoom toggle (Dag/Week/2 weken) | ✅ | item 8 view-switch fix. |
| Week navigation chevrons + "Vandaag" | ✅ | Day-zoom routes prev/next to `previousDay/nextDay`. |
| "Open shifts delen (N)" header button | ✅ | Now placed BEFORE week-nav (item 12) so the nav doesn't jump. |
| Day-cells with weekend styling | ✅ | `nonWorkingTimeFeature=true` + `.b-weekend-cell` rule in scss. |
| "Vandaag" magenta stripe + column header | ✅ | `today-range` time-range + `.b-sch-dayheadercell-today` override. |
| Now-line in Day zoom | ✅ | `poc-now-line` timeRange. |
| Branch-row chrome (vestiging name + actions) | ✅ | gear+plus collapsed into 3-dot menu (item 1). |
| `opted-badge` (applications count) on open-shift blocks | ✅ | `poc-open-badge`, only painted when `applications_count > 0`. |
| `md-block.fixed` (Vast block) | ✅ | `poc-event-permanent`, teal. |
| Lane stacking with `lane-2` | ⚠️ | We rely on Bryntum `allowOverlap=true` rather than explicit lane indexes — visually equivalent for this PoC, but pixel-perfect mockup 11 stacking isn't enforced. |
| Open-shift dashed border | ✅ | `poc-event-shift` SCSS. |
| Service-location sub-row indent | ✅ | `poc-sl-row` padding-left. |

## Mockup 15 — Pool MyStaffler

PoC entry: `pool/`.

| Element | Status | Notes |
|---|---|---|
| Columns: Medewerker / Vestiging / MyStaffler account / Last login / acties | ✅ | Same order. |
| Per-row 3-dot action menu | ✅ | `actions-btn` equivalent in impl. |
| MyStaffler account states | ⚠️ | Mockup distinguishes 3 states: `active` (green), `pending` (amber), `invite` (CTA). Impl has `active` / `invited` (amber "Uitgenodigd") / default ("Uitnodigen" CTA) — equivalent semantics, slightly different labels. |
| Last login column | ✅ | Now live — `touchMyStafflerLogin` bumps every active invite when the operator opens the employee's MyStaffler preview (gap 3). |
| Empty state for null last_login | ⚠️ | Impl shows "Nooit" for any null. Mockup is empty for non-active rows. "Nooit" is more explicit, keep as-is. |
| Pool filter chips (Iedereen / Actief / Uitgenodigd) | ✅ | Chip bar with counts at top of page. |

## Mockup 06 — Selectie-picker (full-screen)

PoC entry: `dialog-shift-share/` (inline picker, not full-screen).

| Element | Status | Notes |
|---|---|---|
| Statuut filter chips | 🔵 | Not implemented. |
| Group filter chips | 🔵 | Not implemented. |
| "Beschikbaar deze week" toggle | ✅ | Gap 1 — added during this round. |
| Bulk-add "+ Beschikbaar (N)" chip | ✅ | Gap 1. |
| Bulk-add "+ Alle flexi's", "+ Groep X" chips | 🔵 | Not implemented (no statuut/group data plumbed in). |
| Per-row statuut tag + groups | 🔵 | List shows name only. |
| Per-row availability badge for the specific shift hours | 🔵 | "Available this week" is week-scoped, not hour-scoped. |
| Full-screen with selected-bar of chips | 🔵 | Impl is inline within the share dialog. |

## Mockup 14 — Locatie eigenschappen

PoC entry: `company-locations/`, `dialog-add-service-location/`,
`dialog-edit-vestiging/`.

| Element | Status | Notes |
|---|---|---|
| Naam, vestiging, adres editor | ✅ | |
| Per-weekday opening hours editor | ✅ | Gap 2 — added during this round. |
| 7-pill opening-hours strip in table | ✅ | Gap 2. |
| Map preview / Google Maps autocomplete | 🔵 | `dialog-edit-vestiging` has the autocomplete; not reused inside the service-location dialog. Concrete v1 enhancement. |
| Lat/long pinning, route from BOEMM HQ etc. | 🔵 | Out of scope for the PoC. |

## Mobile MyStaffler v2

PoC entry: `mystaffler-preview/` (embedded strip inside company portal,
per PLAN.md — no separate app).

| Element | Status | Notes |
|---|---|---|
| Tab strip: Planning / Beschikbaarheid / Shifts | ✅ | |
| Apply on a broadcast shift | ✅ | |
| Withdraw from a candidacy | ✅ | |
| Add availability (per day) | ✅ | Gap 4 round 1 — Create wired. |
| Remove availability (per day) | ✅ | Gap 4 round 2 — Delete wired with locked-state guard. |
| Custom hours selector | 🔵 | Hardcoded 09:00–17:00. Concrete v1 enhancement. |
| Pull-to-refresh / live updates | 🔵 | Refresh-on-action only (no websocket). Out of scope. |

## Summary of remaining gaps

Items still open after today's two rounds:

1. **Mockup 06 — full-screen picker** (statuut / group filters,
   per-row hour-aware availability badges, selected-bar with chips).
2. **Mockup 09 — grouped naam-dropdown** (sections per service
   location with counts).
3. **Mockup 14 — map preview** in the service-location dialog.
4. **MyStaffler — custom-hours selector** on the day "+" button.
5. **Niveau-2 candidate flow** — `dialog-shift-detail.select()` uses a
   placeholder wage package, not the real `employeewages` row.

None of these are blockers for a demo; all are concrete v1 work.
