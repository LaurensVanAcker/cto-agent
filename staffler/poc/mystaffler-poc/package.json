{
  "name": "mystaffler-poc",
  "version": "0.1.0",
  "private": true,
  "description": "MyStaffler employee-side PoC — mobile-styled SPA on a unique port that talks to the same Fastify proxy as the company-side planning PoC. Per mockup `staffler/mockups/mobile-mystaffler-v2.html`.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "../node_modules/.bin/tsc -p tsconfig.json",
    "typecheck": "../node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "watch": "../node_modules/.bin/tsc -p tsconfig.json --watch",
    "start": "../node_modules/.bin/tsc -p tsconfig.json && node serve.mjs",
    "dev": "node serve.mjs"
  }
}
